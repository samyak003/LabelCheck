
# Example environment variables for LabelCheck
#
# Copy this file to `.env.local` (or `.env`) and fill in your real values.
# Do NOT commit your real secrets to version control. This repository contains
# only example placeholders.
#
# Vite requires env variables exposed to the client to be prefixed with VITE_.

VITE_apiKey=your-firebase-api-key-here
VITE_authDomain=your-project-id.firebaseapp.com
VITE_projectId=your-project-id
VITE_storageBucket=your-project-id.appspot.com
VITE_messagingSenderId=your-messaging-sender-id
VITE_appId=your-app-id

# API key for Gemini (or other LLM) used by the app. Keep this secret in
# your local environment; do not commit it. Example providers use prefixes like
# "sk-" or similar; include the full key value here when running locally.
VITE_gemini_api_key=your-gemini-api-key-here

# Example: to use this locally create a file named `.env.local` with these
# values filled in, then run `npm run dev` (Vite will load `.env.local`).
